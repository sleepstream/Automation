#token to access to domain
accessToken=${accessToken}

#URLs to domains
baseUrl=${baseUrl}

#credentials for antph
userName=${userName}
domain=${domain}
password=${password}

#properties for UI test
timeOutUI=${timeOutUI}